# Create Script
"""CREATE TABLE swaptracker (
    user_id BIGINT PRIMARY KEY,
    user_name VARCHAR(255),
    swap_tracker_name VARCHAR(255),
    current_swaps INT DEFAULT 0,
    goal INT DEFAULT 0,
    common INT DEFAULT 0,
    uncommon INT DEFAULT 0,
    rare INT DEFAULT 0,
    super_rare INT DEFAULT 0,
    legendary INT DEFAULT 0,
    shiny INT DEFAULT 0,
    golden INT DEFAULT 0
);"""
